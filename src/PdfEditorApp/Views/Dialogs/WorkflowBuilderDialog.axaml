<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:PdfEditorApp.ViewModels"
             xmlns:models="using:PdfEditorApp.Models"
             xmlns:converters="using:PdfEditorApp.Converters"
             xmlns:materialIcons="clr-namespace:Material.Icons.Avalonia;assembly=Material.Icons.Avalonia"
             x:Class="PdfEditorApp.Views.Dialogs.WorkflowBuilderDialog"
             x:DataType="vm:WorkflowBuilderViewModel">

    <UserControl.Resources>
        <converters:HexToBrushConverter x:Key="HexToBrushConverter" />
    </UserControl.Resources>

    <!-- Modal Backdrop Overlay (Transparent so only the rounded card floats) -->
    <Grid Background="Transparent" IsVisible="{Binding IsOpen}">
        <Border Width="920"
                Height="680"
                Background="{DynamicResource WinPanelBrush}"
                BorderBrush="{DynamicResource WinBorderBrush}"
                BorderThickness="1"
                CornerRadius="{StaticResource M3ShapeCornerExtraLarge}"
                BoxShadow="{StaticResource M3ElevationModalDialog}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Margin="24">

            <!-- Inner Rounded Clipping Shell to preserve 28px corners without clipping outer shadow -->
            <Border CornerRadius="{StaticResource M3ShapeCornerExtraLarge}"
                    ClipToBounds="True">
                <Grid RowDefinitions="Auto,*,Auto">

                <!-- 1. Header Bar -->
                <Border Grid.Row="0"
                        Background="{DynamicResource M3TertiaryContainerBrush}"
                        BorderBrush="{DynamicResource M3TertiaryBrush}"
                        BorderThickness="0,0,0,1"
                        Padding="20,16">
                    <Grid ColumnDefinitions="Auto,*,Auto">
                        <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                            <Border Width="40" Height="40" Background="{DynamicResource M3TertiaryBrush}" CornerRadius="{StaticResource M3ShapeCornerMedium}">
                                <materialIcons:MaterialIcon Kind="LightningBoltOutline" Width="22" Height="22" Foreground="{DynamicResource M3OnTertiaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <StackPanel Spacing="2" VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="Automated Workflow Studio" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource M3OnTertiaryContainerBrush}" />
                                    <Border Background="{DynamicResource M3TertiaryBrush}" CornerRadius="{StaticResource M3ShapeCornerExtraSmall}" Padding="6,1">
                                        <TextBlock Text="BATCH PIPELINE" FontSize="9.5" FontWeight="Bold" Foreground="{DynamicResource M3OnTertiaryBrush}" />
                                    </Border>
                                </StackPanel>
                                <TextBlock Text="Build multi-step automated processing pipelines and run batch executions across document sets." FontSize="12" Foreground="{DynamicResource M3OnTertiaryContainerBrush}" />
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                            <Button Command="{Binding CloseCommand}"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Padding="6"
                                    Cursor="Hand"
                                    ToolTip.Tip="Close Studio">
                                <materialIcons:MaterialIcon Kind="Close" Width="18" Height="18" Foreground="{DynamicResource M3OnTertiaryContainerBrush}" />
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- 2. Main Two-Column Layout -->
                <Grid Grid.Row="1" ColumnDefinitions="320,*">

                    <!-- Left Column: Pipeline Steps List -->
                    <Border Grid.Column="0"
                            Background="{DynamicResource WinInputBgBrush}"
                            BorderBrush="{DynamicResource WinBorderBrush}"
                            BorderThickness="0,0,1,0"
                            Padding="16">
                        <Grid RowDefinitions="Auto,*,Auto">
                            <!-- Presets Selector -->
                            <StackPanel Grid.Row="0" Spacing="8" Margin="0,0,0,14">
                                <TextBlock Text="Workflow Preset" FontSize="11.5" FontWeight="SemiBold" Foreground="{DynamicResource WinMutedBrush}" />
                                <ComboBox ItemsSource="{Binding PresetWorkflows}"
                                          SelectedItem="{Binding CurrentWorkflow, Mode=OneWay}"
                                          HorizontalAlignment="Stretch">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate DataType="models:WorkflowDefinition">
                                            <TextBlock Text="{Binding Name}" FontSize="12" />
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </StackPanel>

                            <!-- Step Items Sequence -->
                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                <StackPanel Spacing="8">
                                    <TextBlock Text="Pipeline Execution Steps" FontSize="11.5" FontWeight="Bold" Foreground="{DynamicResource WinTextBrush}" Margin="0,0,0,4" />

                                    <ItemsControl ItemsSource="{Binding Steps}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="models:WorkflowStepDefinition">
                                                <Border Background="{DynamicResource WinPanelBrush}"
                                                        BorderBrush="{DynamicResource WinBorderBrush}"
                                                        BorderThickness="1"
                                                        CornerRadius="{StaticResource M3ShapeCornerMedium}"
                                                        Padding="10,8"
                                                        Margin="0,0,0,6">
                                                    <Grid ColumnDefinitions="Auto,*,Auto">
                                                        <CheckBox Grid.Column="0" IsChecked="{Binding IsEnabled}" Margin="0,0,8,0" VerticalAlignment="Center" />
                                                        <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                                                             <TextBlock Text="{Binding StepName}" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource WinTextBrush}" />
                                                             <TextBlock Text="{Binding StepDescription}" FontSize="10.5" Foreground="{DynamicResource WinMutedBrush}" TextTrimming="CharacterEllipsis" />
                                                        </StackPanel>
                                                        <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="2" VerticalAlignment="Center">
                                                             <Button Command="{Binding $parent[UserControl].((vm:WorkflowBuilderViewModel)DataContext).MoveStepUpCommand}"
                                                                     CommandParameter="{Binding}"
                                                                     Background="Transparent"
                                                                     BorderThickness="0"
                                                                     Padding="3"
                                                                     Cursor="Hand">
                                                                 <materialIcons:MaterialIcon Kind="ChevronUp" Width="14" Height="14" Foreground="{DynamicResource WinMutedBrush}" />
                                                             </Button>
                                                             <Button Command="{Binding $parent[UserControl].((vm:WorkflowBuilderViewModel)DataContext).MoveStepDownCommand}"
                                                                     CommandParameter="{Binding}"
                                                                     Background="Transparent"
                                                                     BorderThickness="0"
                                                                     Padding="3"
                                                                     Cursor="Hand">
                                                                 <materialIcons:MaterialIcon Kind="ChevronDown" Width="14" Height="14" Foreground="{DynamicResource WinMutedBrush}" />
                                                             </Button>
                                                             <Button Command="{Binding $parent[UserControl].((vm:WorkflowBuilderViewModel)DataContext).RemoveStepCommand}"
                                                                     CommandParameter="{Binding}"
                                                                     Background="Transparent"
                                                                     BorderThickness="0"
                                                                     Padding="3"
                                                                     Cursor="Hand">
                                                                 <materialIcons:MaterialIcon Kind="DeleteOutline" Width="14" Height="14" Foreground="{DynamicResource M3ErrorBrush}" />
                                                             </Button>
                                                        </StackPanel>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </ScrollViewer>
                        </Grid>
                    </Border>

                    <!-- Right Column: Add Tools Palette & Input Files -->
                    <ScrollViewer Grid.Column="1" Padding="20" VerticalScrollBarVisibility="Auto">
                        <StackPanel Spacing="18">

                            <!-- Status / Messages -->
                            <Border Background="{DynamicResource M3ErrorContainerBrush}" BorderBrush="{DynamicResource M3ErrorBrush}" BorderThickness="1" CornerRadius="{StaticResource M3ShapeCornerMedium}" Padding="12,8" IsVisible="{Binding HasError}">
                                <TextBlock Text="{Binding ErrorMessage}" FontSize="12" Foreground="{DynamicResource M3OnErrorContainerBrush}" />
                            </Border>

                            <Border Background="{DynamicResource M3SuccessContainerBrush}" BorderBrush="{DynamicResource M3SuccessBrush}" BorderThickness="1" CornerRadius="{StaticResource M3ShapeCornerMedium}" Padding="14,10" IsVisible="{Binding IsComplete}">
                                <StackPanel Orientation="Horizontal" Spacing="10">
                                    <materialIcons:MaterialIcon Kind="CheckCircleOutline" Width="20" Height="20" Foreground="{DynamicResource M3SuccessBrush}" VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding ResultSummary}" FontSize="12" Foreground="{DynamicResource M3OnSuccessContainerBrush}" VerticalAlignment="Center" />
                                </StackPanel>
                            </Border>

                            <!-- Add Steps Tool Palette -->
                            <Border Background="{DynamicResource WinPanelBrush}" BorderBrush="{DynamicResource WinBorderBrush}" BorderThickness="1" CornerRadius="{StaticResource M3ShapeCornerMedium}" Padding="16">
                                <StackPanel Spacing="12">
                                    <TextBlock Text="Add Step to Pipeline" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource WinTextBrush}" />
                                    <ItemsControl ItemsSource="{Binding AvailableTools}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel Orientation="Horizontal" />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="models:PdfToolDefinition">
                                                <Button Command="{Binding $parent[UserControl].((vm:WorkflowBuilderViewModel)DataContext).AddStepCommand}"
                                                        CommandParameter="{Binding}"
                                                        Background="{DynamicResource WinInputBgBrush}"
                                                        BorderBrush="{DynamicResource WinBorderBrush}"
                                                        BorderThickness="1"
                                                        CornerRadius="{StaticResource M3ShapeCornerSmall}"
                                                        Padding="10,6"
                                                        Margin="0,0,8,8"
                                                        Cursor="Hand">
                                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                                        <materialIcons:MaterialIcon Kind="{Binding IconKind}" Width="14" Height="14" Foreground="{Binding IconColorHex, Converter={StaticResource HexToBrushConverter}}" />
                                                        <TextBlock Text="{Binding Name}" FontSize="11.5" FontWeight="Medium" Foreground="{DynamicResource WinTextBrush}" />
                                                    </StackPanel>
                                                </Button>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>

                            <!-- Input Files Batch Section -->
                            <Border Background="{DynamicResource WinPanelBrush}" BorderBrush="{DynamicResource WinBorderBrush}" BorderThickness="1" CornerRadius="{StaticResource M3ShapeCornerMedium}" Padding="16">
                                <StackPanel Spacing="12">
                                    <Grid ColumnDefinitions="*,Auto">
                                        <TextBlock Grid.Column="0" Text="Input Batch Files" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource WinTextBrush}" VerticalAlignment="Center" />
                                        <Button Grid.Column="1"
                                                Classes="accent-btn"
                                                Command="{Binding AddInputFilesCommand}"
                                                Padding="14,0"
                                                Height="30"
                                                FontSize="11.5"
                                                FontWeight="SemiBold">
                                            <StackPanel Orientation="Horizontal" Spacing="6">
                                                <materialIcons:MaterialIcon Kind="Plus" Width="14" Height="14" />
                                                <TextBlock Text="Add PDF Files" VerticalAlignment="Center" />
                                            </StackPanel>
                                        </Button>
                                    </Grid>

                                    <!-- Files List -->
                                    <ItemsControl ItemsSource="{Binding InputFiles}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="x:String">
                                                <Border Background="{DynamicResource WinInputBgBrush}" BorderBrush="{DynamicResource WinBorderBrush}" BorderThickness="1" CornerRadius="{StaticResource M3ShapeCornerSmall}" Padding="8,6" Margin="0,0,0,4">
                                                    <Grid ColumnDefinitions="*,Auto">
                                                        <TextBlock Grid.Column="0" Text="{Binding}" FontSize="11.5" Foreground="{DynamicResource WinTextBrush}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                                                        <Button Grid.Column="1"
                                                                Command="{Binding $parent[UserControl].((vm:WorkflowBuilderViewModel)DataContext).RemoveInputFileCommand}"
                                                                CommandParameter="{Binding}"
                                                                Background="Transparent"
                                                                BorderThickness="0"
                                                                Padding="3"
                                                                Cursor="Hand">
                                                            <materialIcons:MaterialIcon Kind="Close" Width="14" Height="14" Foreground="{DynamicResource WinSubtleBrush}" />
                                                        </Button>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>

                            <!-- Pipeline Execution Progress -->
                            <Border Background="{DynamicResource WinInputBgBrush}" BorderBrush="{DynamicResource WinBorderBrush}" BorderThickness="1" CornerRadius="{StaticResource M3ShapeCornerMedium}" Padding="14,10" IsVisible="{Binding IsRunning}">
                                <StackPanel Spacing="8">
                                    <Grid ColumnDefinitions="*,Auto">
                                        <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource WinTextBrush}" />
                                        <TextBlock Grid.Column="1" Text="{Binding ProgressPercentage, StringFormat='{}{0:F0}%'}" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource M3TertiaryBrush}" />
                                    </Grid>
                                    <ProgressBar Value="{Binding ProgressPercentage}" Minimum="0" Maximum="100" Height="6" CornerRadius="{StaticResource M3ShapeCornerExtraSmall}" Foreground="{DynamicResource M3TertiaryBrush}" />
                                </StackPanel>
                            </Border>

                        </StackPanel>
                    </ScrollViewer>
                </Grid>

                <!-- 3. Bottom Action Bar -->
                <Border Grid.Row="2"
                        Background="{DynamicResource WinInputBgBrush}"
                        BorderBrush="{DynamicResource WinBorderBrush}"
                        BorderThickness="0,1,0,0"
                        Padding="20,14">
                    <Grid ColumnDefinitions="*,Auto">
                        <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <TextBlock Text="{Binding InputFiles.Count, StringFormat='{}{0} input files queued'}" FontSize="12" Foreground="{DynamicResource WinMutedBrush}" />
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="10">
                            <Button Command="{Binding CancelWorkflowCommand}"
                                    Classes="secondary-btn"
                                    Height="34"
                                    Padding="14,0"
                                    FontSize="12"
                                    FontWeight="SemiBold"
                                    IsVisible="{Binding IsRunning}">
                                Cancel
                            </Button>
                            <Button Command="{Binding CloseCommand}"
                                    Classes="secondary-btn"
                                    Height="34"
                                    Padding="14,0"
                                    FontSize="12"
                                    FontWeight="SemiBold"
                                    IsVisible="{Binding !IsRunning}">
                                Close
                            </Button>
                            <Button Command="{Binding RunWorkflowCommand}"
                                    Classes="primary-btn"
                                    Height="34"
                                    Padding="18,0"
                                    FontSize="12.5"
                                    FontWeight="Bold"
                                    IsEnabled="{Binding !IsRunning}">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <materialIcons:MaterialIcon Kind="Play" Width="16" Height="16" />
                                    <TextBlock Text="Run Pipeline" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

            </Grid>
            </Border>
        </Border>
    </Grid>
</UserControl>
